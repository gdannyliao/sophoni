# 桌面 Agent MVP 基础骨架 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭建一个可运行、可测试的 macOS Tauri + Svelte 桌面 Agent 基础骨架，支持 mock Agent 任务流、SQLite 数据地基、工作区文件工具、命令风险分类和三栏工作台。

**Architecture:** 第一阶段把产品拆成 Tauri Desktop Shell 与 Rust Core Runtime。Core Runtime 先提供稳定领域类型、SQLite schema、文件工具、命令风险分类和 mock Agent 事件流；Svelte 前端通过 Tauri command 调用后端并渲染三栏工作台。GLM 真连接入、高级结构索引和移动端安全网关不在本计划内，后续以独立计划实现。

**Tech Stack:** Tauri 2、Rust、Svelte、TypeScript、Vite、SQLite、pnpm、Vitest、Cargo tests。

---

## 范围说明

设计规格覆盖多个子系统，本计划只实现第一个可验收基础层：

- 创建 Tauri + Svelte + TypeScript 工程。
- 建立 Rust Core Runtime 模块边界。
- 建立 SQLite schema 和 repository 骨架。
- 实现工作区内文件读写、快照和 diff 的最小闭环。
- 实现命令风险分类，不实际开放任意命令执行。
- 实现 mock Agent 任务流，模拟“搜索 -> 写文件 -> diff -> 总结”。
- 实现三栏工作台 UI，展示会话、工具日志、文件变更和风险提示。

后续计划再实现：

- GLM Provider 真连接入。
- Function Calling 工具 schema。
- 结构索引解析器。
- 真实命令执行器。
- Keychain 配置页。
- 移动端本机访问层。

## 文件结构

本计划会创建或修改以下文件：

- `package.json`：前端脚本、依赖和测试命令。
- `pnpm-lock.yaml`：pnpm 锁文件。
- `index.html`：Vite 入口。
- `vite.config.ts`：Svelte + Vitest 配置。
- `tsconfig.json`、`tsconfig.node.json`：TypeScript 配置。
- `src/main.ts`：Svelte 启动入口。
- `src/app.css`：全局桌面工作台样式。
- `src/App.svelte`：三栏工作台根组件。
- `src/lib/api.ts`：前端调用 Tauri command 的 API 包装。
- `src/lib/types.ts`：前端共享 DTO 类型。
- `src/lib/mockApi.ts`：浏览器测试环境下的 mock API。
- `src/lib/components/Sidebar.svelte`：左栏工作区和会话列表。
- `src/lib/components/Conversation.svelte`：中栏会话和任务流。
- `src/lib/components/ContextPanel.svelte`：右栏文件、diff、工具日志。
- `src/lib/components/SettingsPanel.svelte`：基础设置面板。
- `src/lib/components/*.test.ts`：组件行为测试。
- `src-tauri/Cargo.toml`：Rust 依赖。
- `src-tauri/tauri.conf.json`：Tauri 配置。
- `src-tauri/build.rs`：Tauri build script。
- `src-tauri/src/lib.rs`：Tauri command 注册。
- `src-tauri/src/main.rs`：桌面应用入口。
- `src-tauri/src/core/mod.rs`：Core Runtime 模块出口。
- `src-tauri/src/core/domain.rs`：领域类型。
- `src-tauri/src/core/errors.rs`：统一错误类型。
- `src-tauri/src/core/storage.rs`：SQLite schema 与 repository。
- `src-tauri/src/core/workspace.rs`：工作区路径和文件工具。
- `src-tauri/src/core/command_risk.rs`：命令风险分类。
- `src-tauri/src/core/agent.rs`：mock Agent 执行器。
- `src-tauri/src/core/diff.rs`：文本 diff 生成。
- `src-tauri/src/core/tests.rs`：Rust Core 单元测试。
- `docs/superpowers/specs/2026-06-13-desktop-agent-design.md`：已确认设计规格，只作为参考，不修改。

## Task 1: 创建 Tauri + Svelte 工程骨架

**Files:**
- Create: `package.json`
- Create: `index.html`
- Create: `vite.config.ts`
- Create: `tsconfig.json`
- Create: `tsconfig.node.json`
- Create: `src/main.ts`
- Create: `src/App.svelte`
- Create: `src/app.css`
- Create: `src-tauri/Cargo.toml`
- Create: `src-tauri/tauri.conf.json`
- Create: `src-tauri/build.rs`
- Create: `src-tauri/src/main.rs`
- Create: `src-tauri/src/lib.rs`
- Modify: `.gitignore`

- [ ] **Step 1: 用官方脚手架生成基础项目**

Run:

```bash
pnpm create tauri-app . --template svelte-ts --manager pnpm
```

Expected:

```text
Project created
```

If the command refuses to scaffold into a non-empty directory, create a temporary directory and copy generated app files back without overwriting `docs/`:

```bash
tmpdir="$(mktemp -d)"
pnpm create tauri-app "$tmpdir/sophoni" --template svelte-ts --manager pnpm
cp -R "$tmpdir/sophoni"/. .
rm -rf "$tmpdir"
```

- [ ] **Step 2: 固定基础 package 脚本**

Replace `package.json` scripts with:

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  }
}
```

Keep dependency versions generated by the scaffold. Do not hand-edit generated version pins unless install fails.

- [ ] **Step 3: 加入测试依赖**

Run:

```bash
pnpm add -D vitest @testing-library/svelte @testing-library/jest-dom jsdom svelte-check
```

Expected:

```text
devDependencies:
+ vitest
+ @testing-library/svelte
+ @testing-library/jest-dom
+ jsdom
+ svelte-check
```

- [ ] **Step 4: 配置 Vitest**

Modify `vite.config.ts` so it exports this shape:

```ts
import { defineConfig } from "vitest/config";
import { svelte } from "@sveltejs/vite-plugin-svelte";

export default defineConfig({
  plugins: [svelte()],
  clearScreen: false,
  server: {
    strictPort: true,
  },
  envPrefix: ["VITE_", "TAURI_"],
  test: {
    environment: "jsdom",
    globals: true,
    setupFiles: ["./src/test/setup.ts"],
  },
});
```

- [ ] **Step 5: 创建测试 setup**

Create `src/test/setup.ts`:

```ts
import "@testing-library/jest-dom/vitest";
```

- [ ] **Step 6: 验证基础工程**

Run:

```bash
pnpm install
pnpm check
pnpm test
pnpm build
cargo test --manifest-path src-tauri/Cargo.toml
```

Expected:

```text
pnpm check exits 0
pnpm test exits 0
pnpm build exits 0
cargo test exits 0
```

- [ ] **Step 7: 提交工程骨架**

```bash
git add package.json pnpm-lock.yaml index.html vite.config.ts tsconfig.json tsconfig.node.json src src-tauri .gitignore
git commit -m "chore: scaffold tauri svelte app"
```

## Task 2: 定义 Core Runtime 领域类型和错误模型

**Files:**
- Create: `src-tauri/src/core/mod.rs`
- Create: `src-tauri/src/core/domain.rs`
- Create: `src-tauri/src/core/errors.rs`
- Modify: `src-tauri/src/lib.rs`
- Test: `src-tauri/src/core/tests.rs`

- [ ] **Step 1: 写失败测试**

Create `src-tauri/src/core/tests.rs`:

```rust
use super::command_risk::{classify_command, CommandRisk};
use super::domain::{TaskStatus, ToolKind};

#[test]
fn task_status_is_serialized_as_snake_case() {
    let value = serde_json::to_value(TaskStatus::WaitingForRiskDecision).unwrap();
    assert_eq!(value, "waiting_for_risk_decision");
}

#[test]
fn tool_kind_is_serialized_as_snake_case() {
    let value = serde_json::to_value(ToolKind::FileWrite).unwrap();
    assert_eq!(value, "file_write");
}

#[test]
fn git_diff_is_low_risk() {
    let risk = classify_command("git diff", "/tmp/project");
    assert_eq!(risk, CommandRisk::Low);
}
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```bash
cargo test --manifest-path src-tauri/Cargo.toml core::tests -q
```

Expected: FAIL because `core`, `domain`, and `command_risk` are not defined.

- [ ] **Step 3: 增加 Rust 依赖**

Modify `src-tauri/Cargo.toml` dependencies:

```toml
[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
uuid = { version = "1", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
rusqlite = { version = "0.32", features = ["bundled", "chrono", "uuid"] }
similar = "2"
walkdir = "2"
```

Keep existing scaffold dependencies if they include additional Tauri plugins.

- [ ] **Step 4: 创建模块出口**

Create `src-tauri/src/core/mod.rs`:

```rust
pub mod agent;
pub mod command_risk;
pub mod diff;
pub mod domain;
pub mod errors;
pub mod storage;
pub mod workspace;

#[cfg(test)]
mod tests;
```

- [ ] **Step 5: 定义领域类型**

Create `src-tauri/src/core/domain.rs`:

```rust
use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
use uuid::Uuid;

#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum TaskStatus {
    Pending,
    Running,
    WaitingForRiskDecision,
    Completed,
    Failed,
    Interrupted,
    Cancelled,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum ToolKind {
    FileSearch,
    FileRead,
    FileWrite,
    Command,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum ChangeKind {
    Created,
    Modified,
    Deleted,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Workspace {
    pub id: Uuid,
    pub name: String,
    pub path: String,
    pub last_opened_at: DateTime<Utc>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct Conversation {
    pub id: Uuid,
    pub workspace_id: Uuid,
    pub title: String,
    pub created_at: DateTime<Utc>,
    pub updated_at: DateTime<Utc>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct TaskRun {
    pub id: Uuid,
    pub conversation_id: Uuid,
    pub status: TaskStatus,
    pub summary: Option<String>,
    pub created_at: DateTime<Utc>,
    pub updated_at: DateTime<Utc>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ToolCall {
    pub id: Uuid,
    pub task_run_id: Uuid,
    pub kind: ToolKind,
    pub name: String,
    pub input_json: String,
    pub output_summary: String,
    pub created_at: DateTime<Utc>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FileChange {
    pub id: Uuid,
    pub task_run_id: Uuid,
    pub path: String,
    pub kind: ChangeKind,
    pub diff: String,
    pub created_at: DateTime<Utc>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct AgentEvent {
    pub kind: String,
    pub title: String,
    pub body: String,
}
```

- [ ] **Step 6: 定义错误类型**

Create `src-tauri/src/core/errors.rs`:

```rust
use thiserror::Error;

#[derive(Debug, Error)]
pub enum AppError {
    #[error("workspace path is outside allowed root: {0}")]
    OutsideWorkspace(String),
    #[error("file is too large: {0} bytes")]
    FileTooLarge(u64),
    #[error("io error: {0}")]
    Io(#[from] std::io::Error),
    #[error("sqlite error: {0}")]
    Sqlite(#[from] rusqlite::Error),
    #[error("serialization error: {0}")]
    Serde(#[from] serde_json::Error),
    #[error("{0}")]
    Message(String),
}

pub type AppResult<T> = Result<T, AppError>;

impl serde::Serialize for AppError {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(&self.to_string())
    }
}
```

- [ ] **Step 7: 添加最小 command_risk 实现**

Create `src-tauri/src/core/command_risk.rs`:

```rust
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum CommandRisk {
    Low,
    High,
}

pub fn classify_command(command: &str, _workspace_root: &str) -> CommandRisk {
    let trimmed = command.trim();
    if matches!(trimmed, "git diff" | "git status" | "ls") || trimmed.starts_with("rg ") {
        CommandRisk::Low
    } else {
        CommandRisk::High
    }
}
```

- [ ] **Step 8: 创建空模块以通过编译**

Create empty files:

```bash
touch src-tauri/src/core/agent.rs src-tauri/src/core/diff.rs src-tauri/src/core/storage.rs src-tauri/src/core/workspace.rs
```

- [ ] **Step 9: 注册 core 模块**

Modify `src-tauri/src/lib.rs`:

```rust
mod core;

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_opener::init())
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

- [ ] **Step 10: 运行测试确认通过**

Run:

```bash
cargo test --manifest-path src-tauri/Cargo.toml core::tests -q
```

Expected:

```text
3 passed
```

- [ ] **Step 11: 提交领域模型**

```bash
git add src-tauri/Cargo.toml src-tauri/src
git commit -m "feat: add core domain model"
```

## Task 3: 建立 SQLite schema 和 repository 骨架

**Files:**
- Modify: `src-tauri/src/core/storage.rs`
- Modify: `src-tauri/src/core/tests.rs`

- [ ] **Step 1: 写失败测试**

Append to `src-tauri/src/core/tests.rs`:

```rust
use super::storage::Storage;

#[test]
fn storage_initializes_schema_and_creates_workspace() {
    let storage = Storage::open_in_memory().unwrap();
    let workspace = storage.create_workspace("Demo", "/tmp/demo").unwrap();
    let loaded = storage.list_workspaces().unwrap();

    assert_eq!(loaded.len(), 1);
    assert_eq!(loaded[0].id, workspace.id);
    assert_eq!(loaded[0].name, "Demo");
    assert_eq!(loaded[0].path, "/tmp/demo");
}
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```bash
cargo test --manifest-path src-tauri/Cargo.toml storage_initializes_schema_and_creates_workspace -q
```

Expected: FAIL because `Storage` is not implemented.

- [ ] **Step 3: 实现 Storage**

Replace `src-tauri/src/core/storage.rs`:

```rust
use chrono::{DateTime, Utc};
use rusqlite::{params, Connection};
use uuid::Uuid;

use super::domain::Workspace;
use super::errors::AppResult;

pub struct Storage {
    conn: Connection,
}

impl Storage {
    pub fn open(path: impl AsRef<std::path::Path>) -> AppResult<Self> {
        let conn = Connection::open(path)?;
        let storage = Self { conn };
        storage.migrate()?;
        Ok(storage)
    }

    pub fn open_in_memory() -> AppResult<Self> {
        let conn = Connection::open_in_memory()?;
        let storage = Self { conn };
        storage.migrate()?;
        Ok(storage)
    }

    fn migrate(&self) -> AppResult<()> {
        self.conn.execute_batch(
            r#"
            CREATE TABLE IF NOT EXISTS workspaces (
                id TEXT PRIMARY KEY NOT NULL,
                name TEXT NOT NULL,
                path TEXT NOT NULL UNIQUE,
                last_opened_at TEXT NOT NULL
            );

            CREATE TABLE IF NOT EXISTS conversations (
                id TEXT PRIMARY KEY NOT NULL,
                workspace_id TEXT NOT NULL,
                title TEXT NOT NULL,
                created_at TEXT NOT NULL,
                updated_at TEXT NOT NULL,
                FOREIGN KEY(workspace_id) REFERENCES workspaces(id)
            );

            CREATE TABLE IF NOT EXISTS task_runs (
                id TEXT PRIMARY KEY NOT NULL,
                conversation_id TEXT NOT NULL,
                status TEXT NOT NULL,
                summary TEXT,
                created_at TEXT NOT NULL,
                updated_at TEXT NOT NULL,
                FOREIGN KEY(conversation_id) REFERENCES conversations(id)
            );

            CREATE TABLE IF NOT EXISTS tool_calls (
                id TEXT PRIMARY KEY NOT NULL,
                task_run_id TEXT NOT NULL,
                kind TEXT NOT NULL,
                name TEXT NOT NULL,
                input_json TEXT NOT NULL,
                output_summary TEXT NOT NULL,
                created_at TEXT NOT NULL,
                FOREIGN KEY(task_run_id) REFERENCES task_runs(id)
            );

            CREATE TABLE IF NOT EXISTS file_changes (
                id TEXT PRIMARY KEY NOT NULL,
                task_run_id TEXT NOT NULL,
                path TEXT NOT NULL,
                kind TEXT NOT NULL,
                diff TEXT NOT NULL,
                created_at TEXT NOT NULL,
                FOREIGN KEY(task_run_id) REFERENCES task_runs(id)
            );
            "#,
        )?;
        Ok(())
    }

    pub fn create_workspace(&self, name: &str, path: &str) -> AppResult<Workspace> {
        let workspace = Workspace {
            id: Uuid::new_v4(),
            name: name.to_string(),
            path: path.to_string(),
            last_opened_at: Utc::now(),
        };

        self.conn.execute(
            "INSERT INTO workspaces (id, name, path, last_opened_at) VALUES (?1, ?2, ?3, ?4)",
            params![
                workspace.id.to_string(),
                workspace.name,
                workspace.path,
                workspace.last_opened_at.to_rfc3339()
            ],
        )?;

        Ok(workspace)
    }

    pub fn list_workspaces(&self) -> AppResult<Vec<Workspace>> {
        let mut stmt = self.conn.prepare(
            "SELECT id, name, path, last_opened_at FROM workspaces ORDER BY last_opened_at DESC",
        )?;
        let rows = stmt.query_map([], |row| {
            let id: String = row.get(0)?;
            let last_opened_at: String = row.get(3)?;
            Ok(Workspace {
                id: Uuid::parse_str(&id).expect("workspace id should be a uuid"),
                name: row.get(1)?,
                path: row.get(2)?,
                last_opened_at: DateTime::parse_from_rfc3339(&last_opened_at)
                    .expect("workspace timestamp should be rfc3339")
                    .with_timezone(&Utc),
            })
        })?;

        let mut workspaces = Vec::new();
        for row in rows {
            workspaces.push(row?);
        }
        Ok(workspaces)
    }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run:

```bash
cargo test --manifest-path src-tauri/Cargo.toml storage_initializes_schema_and_creates_workspace -q
```

Expected:

```text
1 passed
```

- [ ] **Step 5: 提交存储骨架**

```bash
git add src-tauri/src/core/storage.rs src-tauri/src/core/tests.rs
git commit -m "feat: add sqlite storage skeleton"
```

## Task 4: 实现工作区文件工具、快照和 diff

**Files:**
- Modify: `src-tauri/src/core/workspace.rs`
- Modify: `src-tauri/src/core/diff.rs`
- Modify: `src-tauri/src/core/tests.rs`

- [ ] **Step 1: 写失败测试**

Append to `src-tauri/src/core/tests.rs`:

```rust
use super::workspace::WorkspaceFs;

#[test]
fn workspace_rejects_paths_outside_root() {
    let root = std::env::temp_dir().join(format!("sophoni-test-{}", uuid::Uuid::new_v4()));
    std::fs::create_dir_all(&root).unwrap();

    let fs = WorkspaceFs::new(root.clone());
    let outside = root.parent().unwrap().join("outside.txt");
    let err = fs.read_text(&outside).unwrap_err().to_string();

    assert!(err.contains("outside allowed root"));
    std::fs::remove_dir_all(root).unwrap();
}

#[test]
fn workspace_writes_file_and_returns_diff() {
    let root = std::env::temp_dir().join(format!("sophoni-test-{}", uuid::Uuid::new_v4()));
    std::fs::create_dir_all(&root).unwrap();
    let file = root.join("hello.txt");
    std::fs::write(&file, "hello\n").unwrap();

    let fs = WorkspaceFs::new(root.clone());
    let result = fs.write_text_with_snapshot(&file, "hello world\n").unwrap();

    assert_eq!(result.previous_text, "hello\n");
    assert!(result.diff.contains("-hello"));
    assert!(result.diff.contains("+hello world"));
    assert_eq!(std::fs::read_to_string(&file).unwrap(), "hello world\n");

    std::fs::remove_dir_all(root).unwrap();
}
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```bash
cargo test --manifest-path src-tauri/Cargo.toml workspace_ -q
```

Expected: FAIL because `WorkspaceFs` is not implemented.

- [ ] **Step 3: 实现文本 diff**

Replace `src-tauri/src/core/diff.rs`:

```rust
use similar::{ChangeTag, TextDiff};

pub fn unified_diff(old: &str, new: &str) -> String {
    let diff = TextDiff::from_lines(old, new);
    let mut output = String::new();

    for change in diff.iter_all_changes() {
        let prefix = match change.tag() {
            ChangeTag::Delete => "-",
            ChangeTag::Insert => "+",
            ChangeTag::Equal => " ",
        };
        output.push_str(prefix);
        output.push_str(change.value());
    }

    output
}
```

- [ ] **Step 4: 实现工作区文件工具**

Replace `src-tauri/src/core/workspace.rs`:

```rust
use std::path::{Path, PathBuf};

use super::diff::unified_diff;
use super::errors::{AppError, AppResult};

#[derive(Debug, Clone)]
pub struct WriteResult {
    pub previous_text: String,
    pub next_text: String,
    pub diff: String,
}

#[derive(Debug, Clone)]
pub struct WorkspaceFs {
    root: PathBuf,
}

impl WorkspaceFs {
    pub fn new(root: PathBuf) -> Self {
        Self { root }
    }

    pub fn root(&self) -> &Path {
        &self.root
    }

    pub fn read_text(&self, path: &Path) -> AppResult<String> {
        self.ensure_inside_root(path)?;
        let metadata = std::fs::metadata(path)?;
        if metadata.len() > 1_000_000 {
            return Err(AppError::FileTooLarge(metadata.len()));
        }
        Ok(std::fs::read_to_string(path)?)
    }

    pub fn write_text_with_snapshot(&self, path: &Path, next_text: &str) -> AppResult<WriteResult> {
        self.ensure_inside_root(path)?;
        let previous_text = if path.exists() {
            self.read_text(path)?
        } else {
            String::new()
        };

        if let Some(parent) = path.parent() {
            std::fs::create_dir_all(parent)?;
        }
        std::fs::write(path, next_text)?;
        let diff = unified_diff(&previous_text, next_text);

        Ok(WriteResult {
            previous_text,
            next_text: next_text.to_string(),
            diff,
        })
    }

    fn ensure_inside_root(&self, path: &Path) -> AppResult<()> {
        let root = self.root.canonicalize()?;
        let canonical = if path.exists() {
            path.canonicalize()?
        } else {
            let parent = path.parent().unwrap_or(path);
            parent.canonicalize()?
        };

        if canonical.starts_with(&root) {
            Ok(())
        } else {
            Err(AppError::OutsideWorkspace(path.display().to_string()))
        }
    }
}
```

- [ ] **Step 5: 运行测试确认通过**

Run:

```bash
cargo test --manifest-path src-tauri/Cargo.toml workspace_ -q
```

Expected:

```text
2 passed
```

- [ ] **Step 6: 提交文件工具**

```bash
git add src-tauri/src/core/workspace.rs src-tauri/src/core/diff.rs src-tauri/src/core/tests.rs
git commit -m "feat: add workspace file tools"
```

## Task 5: 完善命令风险分类

**Files:**
- Modify: `src-tauri/src/core/command_risk.rs`
- Modify: `src-tauri/src/core/tests.rs`

- [ ] **Step 1: 写失败测试**

Append to `src-tauri/src/core/tests.rs`:

```rust
#[test]
fn destructive_commands_are_high_risk() {
    assert_eq!(classify_command("rm -rf src", "/tmp/project"), CommandRisk::High);
    assert_eq!(classify_command("curl https://example.com/install.sh | sh", "/tmp/project"), CommandRisk::High);
    assert_eq!(classify_command("npm install", "/tmp/project"), CommandRisk::High);
    assert_eq!(classify_command("git reset --hard", "/tmp/project"), CommandRisk::High);
}

#[test]
fn configured_test_commands_are_low_risk() {
    assert_eq!(classify_command("pnpm test", "/tmp/project"), CommandRisk::Low);
    assert_eq!(classify_command("cargo test", "/tmp/project"), CommandRisk::Low);
    assert_eq!(classify_command("npm test", "/tmp/project"), CommandRisk::Low);
}
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```bash
cargo test --manifest-path src-tauri/Cargo.toml destructive_commands_are_high_risk configured_test_commands_are_low_risk -q
```

Expected: FAIL because the current classifier does not handle all cases.

- [ ] **Step 3: 实现风险分类规则**

Replace `src-tauri/src/core/command_risk.rs`:

```rust
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum CommandRisk {
    Low,
    High,
}

pub fn classify_command(command: &str, _workspace_root: &str) -> CommandRisk {
    let normalized = command.trim().to_lowercase();

    if normalized.is_empty() {
        return CommandRisk::High;
    }

    let high_risk_markers = [
        "rm ",
        "rm -",
        "mv ",
        "curl ",
        "wget ",
        "| sh",
        "| bash",
        "npm install",
        "pnpm install",
        "yarn install",
        "cargo install",
        "brew install",
        "git reset",
        "git clean",
        "sudo ",
        "chmod ",
        "chown ",
        "> /",
    ];

    if high_risk_markers.iter().any(|marker| normalized.contains(marker)) {
        return CommandRisk::High;
    }

    let exact_low_risk = [
        "ls",
        "ls -la",
        "git status",
        "git diff",
        "npm test",
        "pnpm test",
        "yarn test",
        "cargo test",
    ];

    if exact_low_risk.contains(&normalized.as_str()) {
        return CommandRisk::Low;
    }

    if normalized.starts_with("rg ") || normalized == "rg" {
        return CommandRisk::Low;
    }

    CommandRisk::High
}
```

- [ ] **Step 4: 运行测试确认通过**

Run:

```bash
cargo test --manifest-path src-tauri/Cargo.toml command -q
```

Expected:

```text
all command risk tests pass
```

- [ ] **Step 5: 提交风险分类**

```bash
git add src-tauri/src/core/command_risk.rs src-tauri/src/core/tests.rs
git commit -m "feat: classify command risk"
```

## Task 6: 实现 mock Agent 执行器

**Files:**
- Modify: `src-tauri/src/core/agent.rs`
- Modify: `src-tauri/src/core/tests.rs`

- [ ] **Step 1: 写失败测试**

Append to `src-tauri/src/core/tests.rs`:

```rust
use super::agent::run_mock_agent_task;

#[test]
fn mock_agent_returns_events_and_file_change() {
    let root = std::env::temp_dir().join(format!("sophoni-agent-test-{}", uuid::Uuid::new_v4()));
    std::fs::create_dir_all(&root).unwrap();

    let result = run_mock_agent_task(root.clone(), "更新 README").unwrap();

    assert!(result.summary.contains("mock Agent"));
    assert!(result.events.iter().any(|event| event.kind == "tool"));
    assert_eq!(result.file_changes.len(), 1);
    assert!(result.file_changes[0].diff.contains("+# Sophoni"));

    std::fs::remove_dir_all(root).unwrap();
}
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```bash
cargo test --manifest-path src-tauri/Cargo.toml mock_agent_returns_events_and_file_change -q
```

Expected: FAIL because `run_mock_agent_task` is not implemented.

- [ ] **Step 3: 实现 mock Agent**

Replace `src-tauri/src/core/agent.rs`:

```rust
use std::path::PathBuf;

use chrono::Utc;
use uuid::Uuid;

use super::domain::{AgentEvent, ChangeKind, FileChange};
use super::errors::AppResult;
use super::workspace::WorkspaceFs;

#[derive(Debug, Clone, serde::Serialize)]
#[serde(rename_all = "camelCase")]
pub struct AgentTaskResult {
    pub summary: String,
    pub events: Vec<AgentEvent>,
    pub file_changes: Vec<FileChange>,
}

pub fn run_mock_agent_task(workspace_root: PathBuf, prompt: &str) -> AppResult<AgentTaskResult> {
    let fs = WorkspaceFs::new(workspace_root.clone());
    let target = workspace_root.join("README.md");
    let next_text = format!("# Sophoni\n\nMock task completed for: {}\n", prompt);
    let write = fs.write_text_with_snapshot(&target, &next_text)?;

    let task_id = Uuid::new_v4();
    let change = FileChange {
        id: Uuid::new_v4(),
        task_run_id: task_id,
        path: "README.md".to_string(),
        kind: ChangeKind::Modified,
        diff: write.diff,
        created_at: Utc::now(),
    };

    Ok(AgentTaskResult {
        summary: "mock Agent 已完成一次文件写入任务。".to_string(),
        events: vec![
            AgentEvent {
                kind: "thought".to_string(),
                title: "理解任务".to_string(),
                body: prompt.to_string(),
            },
            AgentEvent {
                kind: "tool".to_string(),
                title: "写入 README.md".to_string(),
                body: "已写入 README.md 并生成 diff。".to_string(),
            },
            AgentEvent {
                kind: "summary".to_string(),
                title: "任务完成".to_string(),
                body: "mock Agent 已生成可展示的文件变更。".to_string(),
            },
        ],
        file_changes: vec![change],
    })
}
```

- [ ] **Step 4: 运行测试确认通过**

Run:

```bash
cargo test --manifest-path src-tauri/Cargo.toml mock_agent_returns_events_and_file_change -q
```

Expected:

```text
1 passed
```

- [ ] **Step 5: 提交 mock Agent**

```bash
git add src-tauri/src/core/agent.rs src-tauri/src/core/tests.rs
git commit -m "feat: add mock agent runtime"
```

## Task 7: 暴露 Tauri commands

**Files:**
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: 写 command 编译测试**

Run:

```bash
cargo check --manifest-path src-tauri/Cargo.toml
```

Expected before implementation: compile succeeds but app has no useful commands.

- [ ] **Step 2: 实现 Tauri commands**

Replace `src-tauri/src/lib.rs`:

```rust
mod core;

use std::path::PathBuf;

use core::agent::{run_mock_agent_task, AgentTaskResult};
use core::command_risk::{classify_command, CommandRisk};

#[tauri::command]
fn get_app_status() -> String {
    "Sophoni desktop agent foundation is ready".to_string()
}

#[tauri::command]
fn classify_command_risk(command: String, workspace_root: String) -> CommandRisk {
    classify_command(&command, &workspace_root)
}

#[tauri::command]
fn run_mock_task(workspace_root: String, prompt: String) -> Result<AgentTaskResult, core::errors::AppError> {
    run_mock_agent_task(PathBuf::from(workspace_root), &prompt)
}

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_opener::init())
        .invoke_handler(tauri::generate_handler![
            get_app_status,
            classify_command_risk,
            run_mock_task
        ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

- [ ] **Step 3: 运行后端检查**

Run:

```bash
cargo check --manifest-path src-tauri/Cargo.toml
cargo test --manifest-path src-tauri/Cargo.toml -q
```

Expected:

```text
cargo check exits 0
all Rust tests pass
```

- [ ] **Step 4: 提交 Tauri commands**

```bash
git add src-tauri/src/lib.rs
git commit -m "feat: expose core tauri commands"
```

## Task 8: 实现前端 API 类型和 mock API

**Files:**
- Create: `src/lib/types.ts`
- Create: `src/lib/api.ts`
- Create: `src/lib/mockApi.ts`
- Test: `src/lib/api.test.ts`

- [ ] **Step 1: 写失败测试**

Create `src/lib/api.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { runMockTaskInBrowser } from "./mockApi";

describe("mockApi", () => {
  it("returns a mock task with events and file changes", async () => {
    const result = await runMockTaskInBrowser("整理 README");

    expect(result.summary).toContain("mock Agent");
    expect(result.events.some((event) => event.kind === "tool")).toBe(true);
    expect(result.fileChanges[0].path).toBe("README.md");
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```bash
pnpm test src/lib/api.test.ts
```

Expected: FAIL because `mockApi` is not implemented.

- [ ] **Step 3: 定义前端类型**

Create `src/lib/types.ts`:

```ts
export type CommandRisk = "low" | "high";

export interface AgentEvent {
  kind: string;
  title: string;
  body: string;
}

export interface FileChange {
  id: string;
  taskRunId: string;
  path: string;
  kind: "created" | "modified" | "deleted";
  diff: string;
  createdAt: string;
}

export interface AgentTaskResult {
  summary: string;
  events: AgentEvent[];
  fileChanges: FileChange[];
}
```

- [ ] **Step 4: 实现 Tauri API 包装**

Create `src/lib/api.ts`:

```ts
import { invoke } from "@tauri-apps/api/core";
import type { AgentTaskResult, CommandRisk } from "./types";

export async function getAppStatus(): Promise<string> {
  return invoke<string>("get_app_status");
}

export async function classifyCommandRisk(command: string, workspaceRoot: string): Promise<CommandRisk> {
  return invoke<CommandRisk>("classify_command_risk", { command, workspaceRoot });
}

export async function runMockTask(workspaceRoot: string, prompt: string): Promise<AgentTaskResult> {
  return invoke<AgentTaskResult>("run_mock_task", { workspaceRoot, prompt });
}
```

- [ ] **Step 5: 实现浏览器 mock API**

Create `src/lib/mockApi.ts`:

```ts
import type { AgentTaskResult } from "./types";

export async function runMockTaskInBrowser(prompt: string): Promise<AgentTaskResult> {
  return {
    summary: "mock Agent 已完成一次文件写入任务。",
    events: [
      { kind: "thought", title: "理解任务", body: prompt },
      { kind: "tool", title: "写入 README.md", body: "已写入 README.md 并生成 diff。" },
      { kind: "summary", title: "任务完成", body: "mock Agent 已生成可展示的文件变更。" },
    ],
    fileChanges: [
      {
        id: "change-1",
        taskRunId: "task-1",
        path: "README.md",
        kind: "modified",
        diff: " # Sophoni\n+Mock task completed for: " + prompt + "\n",
        createdAt: new Date("2026-06-13T00:00:00.000Z").toISOString(),
      },
    ],
  };
}
```

- [ ] **Step 6: 运行测试确认通过**

Run:

```bash
pnpm test src/lib/api.test.ts
```

Expected:

```text
1 passed
```

- [ ] **Step 7: 提交前端 API**

```bash
git add src/lib/types.ts src/lib/api.ts src/lib/mockApi.ts src/lib/api.test.ts
git commit -m "feat: add frontend agent api types"
```

## Task 9: 实现三栏工作台 UI

**Files:**
- Modify: `src/App.svelte`
- Modify: `src/app.css`
- Create: `src/lib/components/Sidebar.svelte`
- Create: `src/lib/components/Conversation.svelte`
- Create: `src/lib/components/ContextPanel.svelte`
- Create: `src/lib/components/SettingsPanel.svelte`
- Test: `src/App.test.ts`

- [ ] **Step 1: 写失败测试**

Create `src/App.test.ts`:

```ts
import { render, screen } from "@testing-library/svelte";
import { describe, expect, it } from "vitest";
import App from "./App.svelte";

describe("App", () => {
  it("renders the three-column desktop workbench", () => {
    render(App);

    expect(screen.getByRole("complementary", { name: "工作区与会话" })).toBeInTheDocument();
    expect(screen.getByRole("main", { name: "任务会话流" })).toBeInTheDocument();
    expect(screen.getByRole("complementary", { name: "上下文与产物" })).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```bash
pnpm test src/App.test.ts
```

Expected: FAIL because the scaffold UI does not expose the required regions.

- [ ] **Step 3: 实现 Sidebar**

Create `src/lib/components/Sidebar.svelte`:

```svelte
<aside class="sidebar" aria-label="工作区与会话">
  <div class="brand">Sophoni</div>
  <section>
    <h2>工作区</h2>
    <button class="primary" type="button">打开工作区</button>
    <p class="muted">尚未选择真实工作区</p>
  </section>
  <section>
    <h2>会话</h2>
    <button class="list-item active" type="button">MVP 骨架任务</button>
  </section>
  <section>
    <h2>模型</h2>
    <p class="muted">GLM Provider 待配置</p>
  </section>
</aside>
```

- [ ] **Step 4: 实现 Conversation**

Create `src/lib/components/Conversation.svelte`:

```svelte
<script lang="ts">
  import type { AgentEvent } from "../types";

  export let events: AgentEvent[] = [];
  export let summary = "输入任务后，Agent 会在这里展示步骤和结果。";
</script>

<main class="conversation" aria-label="任务会话流">
  <header class="topbar">
    <div>
      <h1>桌面 Agent 工作台</h1>
      <p>macOS MVP 基础骨架</p>
    </div>
  </header>

  <div class="messages">
    {#each events as event}
      <article class="event">
        <span>{event.kind}</span>
        <h3>{event.title}</h3>
        <p>{event.body}</p>
      </article>
    {/each}
    <article class="assistant">
      <h3>结果摘要</h3>
      <p>{summary}</p>
    </article>
  </div>

  <form class="composer">
    <input aria-label="任务输入" placeholder="让 Agent 搜索、修改或解释当前工作区..." />
    <button type="button">运行 mock 任务</button>
  </form>
</main>
```

- [ ] **Step 5: 实现 ContextPanel**

Create `src/lib/components/ContextPanel.svelte`:

```svelte
<script lang="ts">
  import type { FileChange } from "../types";

  export let fileChanges: FileChange[] = [];
</script>

<aside class="context" aria-label="上下文与产物">
  <section>
    <h2>文件与变更</h2>
    {#if fileChanges.length === 0}
      <p class="muted">暂无文件变更</p>
    {:else}
      {#each fileChanges as change}
        <article class="change">
          <h3>{change.path}</h3>
          <pre>{change.diff}</pre>
        </article>
      {/each}
    {/if}
  </section>
  <section>
    <h2>工具日志</h2>
    <p class="muted">mock 工具事件会显示在中栏。</p>
  </section>
  <section>
    <h2>风险提示</h2>
    <p class="muted">高风险命令将在后续任务中接入确认流。</p>
  </section>
</aside>
```

- [ ] **Step 6: 实现 SettingsPanel**

Create `src/lib/components/SettingsPanel.svelte`:

```svelte
<section class="settings" aria-label="设置">
  <h2>设置</h2>
  <label>
    GLM API Key
    <input type="password" placeholder="后续接入 Keychain" />
  </label>
  <label>
    默认模型
    <input value="glm-5.1" readonly />
  </label>
</section>
```

- [ ] **Step 7: 实现 App 根组件**

Replace `src/App.svelte`:

```svelte
<script lang="ts">
  import Sidebar from "./lib/components/Sidebar.svelte";
  import Conversation from "./lib/components/Conversation.svelte";
  import ContextPanel from "./lib/components/ContextPanel.svelte";
  import { runMockTaskInBrowser } from "./lib/mockApi";
  import type { AgentEvent, FileChange } from "./lib/types";

  let events: AgentEvent[] = [];
  let fileChanges: FileChange[] = [];
  let summary = "输入任务后，Agent 会在这里展示步骤和结果。";

  async function runDemo() {
    const result = await runMockTaskInBrowser("生成基础 README");
    events = result.events;
    fileChanges = result.fileChanges;
    summary = result.summary;
  }
</script>

<div class="app-shell">
  <Sidebar />
  <Conversation {events} {summary} />
  <ContextPanel {fileChanges} />
</div>

<button class="floating-run" type="button" on:click={runDemo}>运行 mock 任务</button>
```

- [ ] **Step 8: 实现全局样式**

Replace `src/app.css`:

```css
:root {
  color: #172026;
  background: #f5f7f8;
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  min-width: 960px;
  min-height: 100vh;
}

button,
input {
  font: inherit;
}

.app-shell {
  display: grid;
  grid-template-columns: 260px minmax(420px, 1fr) 360px;
  min-height: 100vh;
}

.sidebar,
.context {
  background: #ffffff;
  border-right: 1px solid #dce3e6;
  padding: 20px;
}

.context {
  border-right: 0;
  border-left: 1px solid #dce3e6;
  overflow: auto;
}

.brand {
  font-size: 22px;
  font-weight: 700;
  margin-bottom: 28px;
}

section + section {
  margin-top: 28px;
}

h1,
h2,
h3,
p {
  margin-top: 0;
}

h2 {
  font-size: 13px;
  text-transform: uppercase;
  color: #52636c;
}

.primary,
.list-item,
.floating-run,
.composer button {
  border: 1px solid #1f6feb;
  background: #1f6feb;
  color: white;
  border-radius: 6px;
  padding: 8px 10px;
  cursor: pointer;
}

.list-item {
  width: 100%;
  text-align: left;
}

.muted {
  color: #667780;
  font-size: 14px;
}

.conversation {
  display: grid;
  grid-template-rows: auto 1fr auto;
  min-width: 0;
}

.topbar {
  padding: 22px 28px;
  border-bottom: 1px solid #dce3e6;
  background: #ffffff;
}

.topbar h1 {
  font-size: 20px;
  margin-bottom: 4px;
}

.messages {
  padding: 24px 28px;
  overflow: auto;
}

.event,
.assistant,
.change {
  background: #ffffff;
  border: 1px solid #dce3e6;
  border-radius: 8px;
  padding: 14px;
  margin-bottom: 12px;
}

.event span {
  color: #1f6feb;
  font-size: 12px;
  text-transform: uppercase;
}

.composer {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 10px;
  padding: 18px 28px;
  border-top: 1px solid #dce3e6;
  background: #ffffff;
}

.composer input,
.settings input {
  width: 100%;
  border: 1px solid #c8d2d8;
  border-radius: 6px;
  padding: 9px 10px;
}

pre {
  white-space: pre-wrap;
  font-size: 12px;
  overflow: auto;
}

.floating-run {
  position: fixed;
  right: 380px;
  bottom: 28px;
}
```

- [ ] **Step 9: 运行测试确认通过**

Run:

```bash
pnpm test src/App.test.ts
pnpm build
```

Expected:

```text
App test passes
Vite build exits 0
```

- [ ] **Step 10: 提交三栏工作台**

```bash
git add src/App.svelte src/app.css src/lib/components src/App.test.ts
git commit -m "feat: add desktop workbench ui"
```

## Task 10: 完成基础骨架验收

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 写 README**

Create or replace `README.md`:

````markdown
# Sophoni

Sophoni 是一个桌面端优先的工作区 AI Agent。当前仓库处于 MVP 基础骨架阶段。

## 技术栈

- Tauri 2
- Svelte
- TypeScript
- Rust
- SQLite

## 本地开发

```bash
pnpm install
pnpm dev
```

运行桌面端：

```bash
pnpm tauri dev
```

运行测试：

```bash
pnpm check
pnpm test
cargo test --manifest-path src-tauri/Cargo.toml
```

## 当前能力

- 三栏桌面工作台。
- Rust Core Runtime 基础领域模型。
- SQLite schema 骨架。
- 工作区文件读写和 diff。
- 命令风险分类。
- mock Agent 任务流。

## 尚未实现

- GLM Provider 真连接入。
- 真实 Function Calling 工具循环。
- 高级结构索引。
- 真实命令执行器。
- macOS Keychain 设置页。
````

- [ ] **Step 2: 运行完整验证**

Run:

```bash
pnpm check
pnpm test
pnpm build
cargo test --manifest-path src-tauri/Cargo.toml
cargo check --manifest-path src-tauri/Cargo.toml
```

Expected:

```text
pnpm check exits 0
pnpm test exits 0
pnpm build exits 0
cargo test exits 0
cargo check exits 0
```

- [ ] **Step 3: 手动启动开发服务器**

Run:

```bash
pnpm dev -- --host 127.0.0.1
```

Expected:

```text
Local: http://127.0.0.1:5173/
```

Open `http://127.0.0.1:5173/` and verify:

- 左栏显示 Sophoni、工作区、会话、模型。
- 中栏显示“桌面 Agent 工作台”。
- 右栏显示“文件与变更”。
- 点击“运行 mock 任务”后，中栏出现三条事件，右栏出现 `README.md` diff。

- [ ] **Step 4: 提交验收文档**

```bash
git add README.md
git commit -m "docs: document foundation workflow"
```

## Self-Review

### Spec coverage

- macOS/Tauri/Svelte 基础工程：Task 1。
- Core Runtime 模块边界：Task 2。
- SQLite 本地存储地基：Task 3。
- 文件读写、快照和 diff：Task 4。
- 命令分级风险策略：Task 5。
- 单 Agent 顺序执行的最小可视化事件流：Task 6。
- Tauri command 桥接：Task 7。
- 前端 DTO 与 API 包装：Task 8。
- 三栏工作台：Task 9。
- 基础验收和 README：Task 10。

未覆盖项已经明确拆到后续计划：GLM 真接入、Function Calling、结构索引、真实命令执行、Keychain、移动端访问层。

### Completion marker scan

未发现未完成标记。所有代码步骤都给出可执行代码或精确命令。

### Type consistency

Rust 侧 `AgentTaskResult`、`AgentEvent`、`FileChange` 使用 camelCase 序列化；前端 `AgentTaskResult`、`AgentEvent`、`FileChange` 字段名与 Rust JSON 输出一致。任务状态命名使用 `waiting_for_risk_decision`，不使用旧状态名。
